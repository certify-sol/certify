import type { ConfirmOptions, TransactionSignature } from "@solana/web3.js";
import type { Provider, SendTxRequest } from "../interfaces";
/**
 * Similar to `send`, but for an array of transactions and signers.
 */
export declare const sendAll: ({ provider, reqs, opts, confirm, }: {
    provider: Provider;
    reqs: SendTxRequest[];
    opts: ConfirmOptions;
    confirm?: boolean | undefined;
}) => Promise<TransactionSignature[]>;
//# sourceMappingURL=sendAll.d.ts.map