import BaseUtils from './utils';
import { FetchOptions } from '../types/base/request';
import { DetaType, CompositeType, ObjectType } from '../types/basic';
import { GetResponse, PutResponse, FetchResponse, DeleteResponse, InsertResponse, UpdateResponse, PutManyResponse } from '../types/base/response';
export default class Base {
    private requests;
    util: BaseUtils;
    constructor(projectKey: string, baseName: string, host?: string);
    put(data: DetaType, key?: string): Promise<PutResponse>;
    get(key: string): Promise<GetResponse>;
    delete(key: string): Promise<DeleteResponse>;
    insert(data: DetaType, key?: string): Promise<InsertResponse>;
    putMany(items: DetaType[]): Promise<PutManyResponse>;
    update(updates: ObjectType, key: string): Promise<UpdateResponse>;
    fetch(query?: CompositeType, options?: FetchOptions): Promise<FetchResponse>;
}
